## *多人协作时，要注意以下几个事项：
### 1.首先git pull某个项目到本地
### 2.在本地修改完某版本的代码后，先git add 新建的文件或者文件夹到本地仓库
### 3.git stash是先缓存下当前自己修改的版本，保留自己的更改独立
### 4. git pull 把当前的远程服务器最新版本更新过来
### 5. git status 看一下现在是否确实更新了
### 6. git stash pop把自己的原版本缓存取出来
### 7. git status 查看是否有回退到自己的版本
### 8. git push 把自己的更新推送到远程服务器上
完成。
